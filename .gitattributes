# ── Default: normalise all text files to LF in the repository ─────────────────
*               text=auto eol=lf

# ── Source code ────────────────────────────────────────────────────────────────
*.py            text eol=lf diff=python
*.toml          text eol=lf
*.lock          text eol=lf
*.json          text eol=lf
*.cfg           text eol=lf
*.ini           text eol=lf
*.txt           text eol=lf

# ── Documentation and markup ───────────────────────────────────────────────────
*.md            text eol=lf diff=markdown
*.rst           text eol=lf

# ── Shell scripts ──────────────────────────────────────────────────────────────
*.sh            text eol=lf

# ── Binary: images and PDFs ────────────────────────────────────────────────────
*.png           binary
*.jpg           binary
*.jpeg          binary
*.gif           binary
*.ico           binary
*.pdf           binary
*.svg           text eol=lf

# ── Generated files: always keep our version on merge conflict ─────────────────
# These are fully regenerated by `python -m scripts.requirements.main` and
# should never be manually merged.
requirements/*/README.md    merge=ours

# ── Export: strip dev-only files from `git archive` releases ──────────────────
.gitignore              export-ignore
.gitattributes          export-ignore
.markdownlint.json      export-ignore
pyproject.toml          export-ignore
poetry.lock             export-ignore
